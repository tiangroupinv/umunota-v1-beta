'use client';
import Link from 'next/link';import {useRouter} from 'next/navigation';import {useState} from 'react';import {createClient} from '@/lib/supabase';
export default function Login(){const router=useRouter();const [email,setEmail]=useState('demo@umunota.rw');const [password,setPassword]=useState('demo-password');const [err,setErr]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setErr('');if(!email.includes('@')||password.length<8){setErr('Enter a valid email and a password with at least 8 characters.');return}const configured=Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL&&process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY);if(!configured){router.push('/dashboard');return}setBusy(true);try{const supabase=createClient();const {error}=await supabase.auth.signInWithPassword({email,password});if(error){setErr(error.message);return}router.replace('/dashboard');router.refresh()}finally{setBusy(false)}}return <main className="authPage"><div className="authBrand"><div className="logoMark large">◷</div><b>UMUNOTA</b><span>People. Time. Solutions.</span></div><form className="authCard" onSubmit={submit}><span className="eyebrow">WELCOME BACK</span><h1>Sign in securely</h1><p>Continue to your tasks, payments and community.</p><div className="field"><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" autoComplete="email"/></div><div className="field"><label>Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" autoComplete="current-password"/></div>{err&&<div className="errorBox">{err}</div>}<button disabled={busy} className="btn btn-gold btn-wide">{busy?'Signing in…':'Sign in →'}</button><div className="authFoot"><span>New to UMUNOTA?</span><Link href="/signup">Create account</Link></div><div className="demoNote">When Supabase keys are configured this uses real server-backed auth sessions. Without keys, the project stays in local demo mode for product testing.</div></form></main>}
