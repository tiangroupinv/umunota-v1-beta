import Link from 'next/link';import {requireAdmin} from '@/lib/admin';import {createAdminSupabaseClient} from '@/lib/supabase-admin';import {ArrowRight,BarChart3,ShieldCheck,UsersRound,WalletCards,ClipboardList} from 'lucide-react';
export const dynamic='force-dynamic';export const revalidate=0;
export default async function AdminOverview(){const{profile}=await requireAdmin();const admin=createAdminSupabaseClient();const[{count:users},{count:runners},{count:kyc},{count:tasks},{count:disputes},{data:revenue},{data:payments}]=await Promise.all([admin.from('profiles').select('id',{count:'exact',head:true}),admin.from('profiles').select('id',{count:'exact',head:true}).eq('role','runner'),admin.from('kyc_applications').select('id',{count:'exact',head:true}).in('status',['pending','needs_review']),admin.from('tasks').select('id',{count:'exact',head:true}).not('status','in','(paid,cancelled)'),admin.from('tasks').select('id',{count:'exact',head:true}).eq('status','disputed'),admin.from('revenue_entries').select('amount_rwf,status').eq('status','recorded'),admin.from('payments').select('amount_rwf,status').eq('status','released')]);const revenueTotal=(revenue||[]).reduce((s:any,r:any)=>s+Number(r.amount_rwf||0),0);const released=(payments||[]).reduce((s:any,r:any)=>s+Number(r.amount_rwf||0),0);return <><header className="adminPageHeader"><div><span className="eyebrow">PLATFORM OPERATIONS</span><h1>Admin overview</h1><p>Welcome {profile.full_name||'Administrator'}. This page is only the summary; operational work now lives in separate admin pages.</p></div><div className="adminPageActions"><Link className="btn btn-gold" href="/admin/kyc"><ShieldCheck size={16}/> Review KYC</Link><Link className="btn btn-dark" href="/admin/reports"><BarChart3 size={16}/> Reports</Link></div></header><section className="adminDataGrid"><article><UsersRound/><span>Users</span><b>{users||0}</b><small>{runners||0} runners</small></article><article><ShieldCheck/><span>KYC queue</span><b>{kyc||0}</b><small>waiting for review</small></article><article><ClipboardList/><span>Active tasks</span><b>{tasks||0}</b><small>{disputes||0} disputes</small></article><article><WalletCards/><span>Released volume</span><b>{released.toLocaleString()} RWF</b><small>{revenueTotal.toLocaleString()} RWF recorded revenue</small></article></section><section className="adminListCard"><div className="section-head"><div><span className="eyebrow">QUICK OPERATIONS</span><h2>Go directly to the work</h2></div></div><div className="adminQuickLinks"><Link href="/admin/users"><b>Users</b><span>Search, review roles and open account details.</span><ArrowRight/></Link><Link href="/admin/kyc"><b>KYC review</b><span>Review pending identity applications and decisions.</span><ArrowRight/></Link><Link href="/admin/tasks"><b>Task operations</b><span>Filter and manage marketplace task states.</span><ArrowRight/></Link><Link href="/admin/payments"><b>Payments</b><span>Inspect provider state and payment lifecycle.</span><ArrowRight/></Link><Link href="/admin/revenue"><b>Revenue</b><span>Service fees, subscriptions and recorded revenue.</span><ArrowRight/></Link><Link href="/admin/ads"><b>Banner ads</b><span>Choose page placement, image URL, CTA and campaign dates.</span><ArrowRight/></Link></div></section></>}
