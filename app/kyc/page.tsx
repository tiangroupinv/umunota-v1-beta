'use client';
import AppShell from '@/components/AppShell';
import Link from 'next/link';
import {useEffect,useRef,useState} from 'react';
import {createClient} from '@/lib/supabase';
import {BadgeCheck,ChevronLeft,ChevronRight,FileCheck2,LoaderCircle,ShieldCheck,Upload,UserRoundCheck} from 'lucide-react';

const CONTRACT_VERSION='2026-09-10';
type Status='not_started'|'pending'|'verified'|'rejected'|'needs_review';
const steps=['Personal information','Identity documents','Marketplace experience','Agreement & contract'];

export default function Kyc(){
 const [status,setStatus]=useState<Status>('not_started');const [step,setStep]=useState(-1);const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [feedback,setFeedback]=useState('');const formRef=useRef<HTMLFormElement>(null);
 useEffect(()=>{(async()=>{const s=createClient();const {data:{user}}=await s.auth.getUser();if(!user){location.href='/login?next=/kyc';return}const {data}=await s.from('profiles').select('kyc_status').eq('id',user.id).single();if(data?.kyc_status)setStatus(data.kyc_status as Status)})()},[]);
 function validateCurrentStep(){const form=formRef.current;if(!form)return false;const section=form.querySelector<HTMLElement>(`[data-kyc-step="${step}"]`);if(!section)return false;const fields=Array.from(section.querySelectorAll<HTMLInputElement|HTMLSelectElement|HTMLTextAreaElement>('input,select,textarea'));for(const field of fields){if(!field.checkValidity()){field.reportValidity();setError('Complete the required fields in this step before continuing.');field.focus();return false}}setError('');return true}
 function next(){if(validateCurrentStep())setStep(s=>Math.min(3,s+1))}
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(busy)return;setError('');setFeedback('Checking your application...');const form=e.currentTarget;const all=Array.from(form.querySelectorAll<HTMLInputElement|HTMLSelectElement|HTMLTextAreaElement>('input,select,textarea'));for(const field of all){if(!field.checkValidity()){setStep(Number(field.closest<HTMLElement>('[data-kyc-step]')?.dataset.kycStep||0));setError('Some required information is missing. Complete the highlighted field and submit again.');setFeedback('');setTimeout(()=>{field.reportValidity();field.focus()},50);return}}
 setBusy(true);setFeedback('Uploading your private documents securely...');try{const payload=new FormData(form);const res=await fetch('/api/kyc/application',{method:'POST',body:payload});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.error||'Unable to submit verification application.');setFeedback('Application submitted successfully. Your account is now waiting for administrator review.');setStatus('pending');setTimeout(()=>setStep(-1),850)}catch(e){setError(e instanceof Error?e.message:'Unable to submit verification application.');setFeedback('')}finally{setBusy(false)}}
 const pending=status==='pending'||status==='needs_review',verified=status==='verified';
 if(step<0)return <AppShell><div className="pageHead"><div><span className="eyebrow">IDENTITY & MARKETPLACE VERIFICATION</span><h1>Verify your UMUNOTA account</h1><p>Complete one secure application. Identity documents remain private and an administrator reviews the submission.</p></div><div className={`kycState ${verified?'verifiedState':''}`}><ShieldCheck size={22}/><span>{status.replaceAll('_',' ')}</span></div></div><div className="kycLayout"><section className="card verificationJourney"><h2>What verification includes</h2><div className="verificationFeature"><UserRoundCheck/><div><b>Personal information</b><p>Legal identity, contact information and basic eligibility information.</p></div></div><div className="verificationFeature"><FileCheck2/><div><b>Private identity-document review</b><p>Upload front and back images. Documents are stored privately and are not shown to marketplace users.</p></div></div><div className="verificationFeature"><BadgeCheck/><div><b>Marketplace understanding</b><p>Tell us how you intend to use UMUNOTA and confirm that you understand task, safety, payment and revenue rules.</p></div></div><div className="verificationFeature"><ShieldCheck/><div><b>Agreement</b><p>Review the marketplace contract, Terms, Privacy Policy, Safety rules and payment responsibilities before submission.</p></div></div></section><aside className="sidecards"><div className="card verificationCard"><span className="eyebrow">CURRENT STATUS</span><h2>{verified?'Verified':pending?'Application under review':'Ready to verify'}</h2><p>{verified?'Your verification has been approved.':pending?'An administrator will review your application. You cannot submit another application while this one is pending.':'Have your identity document ready before starting.'}</p>{!verified&&!pending&&<button className="btn btn-gold btn-wide" onClick={()=>{setError('');setFeedback('');setStep(0)}}>Start verification</button>}{verified&&<Link className="btn btn-gold btn-wide" href="/dashboard">Continue to dashboard</Link>}</div></aside></div></AppShell>;
 return <AppShell><div className="kycWizard"><header><span className="eyebrow">SECURE VERIFICATION</span><h1>{steps[step]}</h1><div className="kycProgress">{steps.map((x,i)=><div key={x} className={i<=step?'active':''}><span>{i+1}</span><small>{x}</small></div>)}</div></header><form ref={formRef} className="card kycForm" onSubmit={submit} noValidate><section data-kyc-step="0" hidden={step!==0}><h2>Personal information</h2><p className="muted">Enter information exactly as it appears on your identity document where applicable.</p><div className="kycFields"><label>Full legal name<input name="full_name" required autoComplete="name"/></label><label>Username<input name="username" required/></label><label>Phone number<input name="phone" type="tel" required autoComplete="tel" placeholder="+250 ..."/></label><label>Date of birth<input name="dob" type="date" required/></label><label>Nationality<input name="nationality" required/></label><label>District / city<input name="district" required/></label><label className="wide">Address / area<input name="address"/></label></div></section><section data-kyc-step="1" hidden={step!==1}><h2>Identity verification</h2><p className="muted">Use a valid identity document belonging to you. Do not upload another person's document.</p><div className="kycFields"><label>Document type<select name="identity_type" required defaultValue=""><option value="" disabled>Select document</option><option>National ID</option><option>Passport</option><option>Other government ID</option></select></label><label>Document number<input name="identity_number" required/></label><label className="kycUpload"><Upload/><b>Front of document</b><small>JPG, PNG, WebP or PDF · max 8 MB</small><input name="id_front" type="file" accept="image/jpeg,image/png,image/webp,application/pdf" required/></label><label className="kycUpload"><Upload/><b>Back of document</b><small>JPG, PNG, WebP or PDF · max 8 MB</small><input name="id_back" type="file" accept="image/jpeg,image/png,image/webp,application/pdf" required/></label></div></section><section data-kyc-step="2" hidden={step!==2}><h2>Your marketplace experience</h2><div className="kycFields"><label>How will you use UMUNOTA?<select name="marketplace_role" required defaultValue=""><option value="" disabled>Select</option><option value="customer">Post tasks as a customer</option><option value="runner">Complete tasks as a runner</option><option value="both">Both</option></select></label><label>Have you done similar task work before?<select name="task_experience" required defaultValue=""><option value="" disabled>Select</option><option>Yes</option><option>No</option><option>Some informal experience</option></select></label><label className="wide">What do you expect from UMUNOTA?<textarea name="work_expectations" required placeholder="Briefly explain how you plan to use the marketplace."/></label><label className="wide">If a task seems unsafe, illegal, misleading or outside the agreed scope, what will you do?<textarea name="safety_answer" required placeholder="Explain how you would respond."/></label></div></section><section data-kyc-step="3" hidden={step!==3}><h2>Marketplace agreement</h2><div className="contractBox"><h3>Before you submit</h3><p>UMUNOTA connects customers and runners for lawful local tasks. Verification does not guarantee earnings, jobs, customers or a particular income. Task budgets and any applicable platform fees must be shown before commitment. Payment release follows the task lifecycle, completion proof, approval and dispute rules. Users remain responsible for applicable personal tax and legal obligations.</p><p>Do not use UMUNOTA for illegal, dangerous, age-restricted, exploitative or prohibited activity. Do not share another person's identity documents. Follow agreed task scope, protect other users' privacy, provide truthful completion proof and use the dispute process when work or payment is contested.</p><p>UMUNOTA may review, restrict or suspend accounts for fraud, safety violations, false identity information, payment abuse or serious policy violations. Verification information is used for trust, safety, fraud prevention and marketplace administration according to the Privacy Policy.</p><p>Contract version: {CONTRACT_VERSION}. Read the complete <Link href="/legal/terms" target="_blank">Terms & Conditions</Link>, <Link href="/legal/privacy" target="_blank">Privacy Policy</Link>, <Link href="/legal/safety" target="_blank">Safety Policy</Link> and <Link href="/legal/payments" target="_blank">Payments Policy</Link>.</p></div><div className="kycChecks"><label><input name="revenue_acknowledged" type="checkbox" required/> <span>I understand that earnings are not guaranteed and that displayed platform fees/revenue rules apply.</span></label><label><input name="payment_acknowledged" type="checkbox" required/> <span>I understand the payment, completion-proof, approval and dispute process.</span></label><label><input name="terms_accepted" type="checkbox" required/> <span>I accept the Terms & Conditions.</span></label><label><input name="privacy_accepted" type="checkbox" required/> <span>I acknowledge the Privacy Policy and identity-data processing described there.</span></label><label><input name="safety_accepted" type="checkbox" required/> <span>I agree to the Safety & Acceptable Use Policy.</span></label></div></section>{error&&<div className="errorBox kycSubmitFeedback">{error}</div>}{feedback&&<div className="notice success kycSubmitFeedback">{busy&&<LoaderCircle size={16} className="spinIcon"/>}{feedback}</div>}<footer className="kycWizardActions"><button type="button" className="btn btn-dark" disabled={busy} onClick={()=>step===0?setStep(-1):setStep(step-1)}><ChevronLeft size={17}/> Back</button>{step<3?<button type="button" className="btn btn-gold" onClick={next}>Continue <ChevronRight size={17}/></button>:<button type="submit" className="btn btn-gold kycSubmitButton" disabled={busy}>{busy?<><LoaderCircle size={17} className="spinIcon"/> Submitting application...</>:'Submit application for approval'}</button>}</footer></form></div></AppShell>;
}
