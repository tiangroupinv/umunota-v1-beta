'use client';
import AppShell from '@/components/AppShell';import {useState} from 'react';
const steps=[['Phone number','Verified','Your number was confirmed with OTP.'],['Email address','Verified','Your email address is confirmed.'],['Government ID','Verified','ID details passed identity review.'],['Selfie & liveness','Verified','Face and liveness check completed.'],['Payment ownership','Pending','Confirm the mobile money or bank account you use.'],['Address & location','Pending','Add your primary service area.']];
export default function Kyc(){const [started,setStarted]=useState(false);return <AppShell><div className="pageHead"><div><span className="eyebrow">TRUST CENTER</span><h1>Identity & verification</h1><p>UMUNOTA separates “submitted” from “verified.” A document upload alone never proves identity.</p></div><div className="trustScoreBig"><b>92</b><span>/100 trust</span></div></div><div className="kycLayout"><section className="card"><div className="section-head"><div><span className="eyebrow">YOUR CHECKS</span><h2>Verification progress</h2></div><span className="verifiedBadge">4 of 6 verified</span></div><div className="checklist">{steps.map(([name,status,desc],i)=><div className="checkItem" key={name}><span className={status==='Verified'?'checkIcon done':'checkIcon'}>{status==='Verified'?'✓':i+1}</span><div><b>{name}</b><p>{desc}</p></div><span className={status==='Verified'?'statusBadge status-paid':'statusBadge status-posted'}>{status}</span></div>)}</div></section><aside className="sidecards"><div className="card verificationCard"><span className="eyebrow">NEXT STEP</span><h2>Payment ownership</h2><p>Confirm that the payout account belongs to you. This reduces impersonation and payout fraud.</p><button onClick={()=>setStarted(true)} className="btn btn-gold btn-wide">{started?'Verification request started ✓':'Continue verification'}</button>{started&&<div className="notice success">Demo only: production requires a real verification/provider callback before this can become “Verified.”</div>}</div><div className="card"><span className="eyebrow">PRIVACY</span><h3>Your ID is not public</h3><p className="muted">Profiles should expose verification status, not raw identity documents. Sensitive files belong in private storage with short-lived signed access and audit logs.</p></div></aside></div></AppShell>}
