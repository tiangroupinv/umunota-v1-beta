'use client';
import AppShell from '@/components/AppShell';import Link from 'next/link';import {useEffect,useState} from 'react';import {createClient} from '@/lib/supabase';import {BadgeCheck,BriefcaseBusiness,LogOut,Settings,ShieldCheck,UserRound,UsersRound} from 'lucide-react';
type Profile={id:string;full_name:string|null;username:string|null;phone:string|null;avatar_url:string|null;role:string;kyc_status:string;rating:number|null;rating_count:number|null;created_at:string};
export default function ProfilePage(){const [profile,setProfile]=useState<Profile|null>(null);const [name,setName]=useState('');const [username,setUsername]=useState('');const [phone,setPhone]=useState('');const [followers,setFollowers]=useState(0);const [following,setFollowing]=useState(0);const [saved,setSaved]=useState(false);const [error,setError]=useState('');const [runnerBusy,setRunnerBusy]=useState(false);useEffect(()=>{(async()=>{try{const s=createClient();const {data:{user}}=await s.auth.getUser();if(!user){location.href='/login?next=/profile';return}const [{data,error},{count:followersCount},{count:followingCount}]=await Promise.all([s.from('profiles').select('id,full_name,username,phone,avatar_url,role,kyc_status,rating,rating_count,created_at').eq('id',user.id).single(),s.from('profile_follows').select('*',{count:'exact',head:true}).eq('following_id',user.id),s.from('profile_follows').select('*',{count:'exact',head:true}).eq('follower_id',user.id)]);if(error)throw error;setProfile(data);setName(data.full_name||'');setUsername(data.username||'');setPhone(data.phone||'');setFollowers(followersCount||0);setFollowing(followingCount||0)}catch(e){setError(e instanceof Error?e.message:'Unable to load profile.')}})()},[]);async function save(){if(!profile)return;const s=createClient();const {error}=await s.from('profiles').update({full_name:name.trim(),username:username.trim()||null,phone:phone.trim()||null,updated_at:new Date().toISOString()}).eq('id',profile.id);if(error){setError(error.message);return}setSaved(true)}async function enableRunner(){if(!profile)return;setRunnerBusy(true);const r=await fetch('/api/profile/runner-mode',{method:'POST'});const d=await r.json();if(r.ok)setProfile({...profile,role:d.role});else if(d.code==='KYC_REQUIRED')location.href='/kyc';setRunnerBusy(false)}async function signOut(){const s=createClient();await s.auth.signOut();location.href='/login'}if(!profile)return <AppShell><div className="card loadingCard">Loading profile...</div></AppShell>;const display=profile.full_name||profile.username||'UMUNOTA member';const initials=display.split(' ').map(x=>x[0]).slice(0,2).join('').toUpperCase();return <AppShell><div className="profileHeader customProfileHeader">{profile.avatar_url?<div className="avatar avatarXXl"><img src={profile.avatar_url} alt=""/></div>:<div className="avatar avatarXXl">{initials}</div>}<div><span className={profile.kyc_status==='verified'?'verifiedBadge':'taskState'}>{profile.kyc_status==='verified'?<><BadgeCheck size={15}/> Identity verified</>:profile.kyc_status.replace('_',' ')}</span><h1>{display}</h1><p>@{profile.username||'member'} · {profile.role} · Member since {new Date(profile.created_at).toLocaleDateString()}</p><div className="profileNumbers"><span><b>{Number(profile.rating||0).toFixed(1)}</b> rating</span><span><b>{profile.rating_count||0}</b> ratings</span><span><b>{followers}</b> followers</span><span><b>{following}</b> following</span></div><Link href="/settings" className="btn btn-dark"><Settings size={16}/> Account settings</Link></div></div><div className="profileLayout"><section className="card formCard customForm"><span className="eyebrow">PUBLIC PROFILE</span><h2>Profile information</h2><div className="field"><label>Full name</label><input value={name} onChange={e=>setName(e.target.value)}/></div><div className="field"><label>Username</label><input value={username} onChange={e=>setUsername(e.target.value)}/></div><div className="field"><label>Phone</label><input value={phone} onChange={e=>setPhone(e.target.value)} type="tel"/></div>{error&&<div className="errorBox">{error}</div>}{saved&&<div className="notice success">Profile updated.</div>}<button onClick={save} className="btn btn-gold">Save changes</button></section><aside className="sidecards"><div className="card"><UsersRound className="goldIcon"/><span className="eyebrow">COMMUNITY</span><h3>{followers} followers · {following} following</h3><p className="muted">People you follow are prioritized in your community feed.</p><Link href="/community" className="btn btn-dark btn-wide">Open community</Link></div><div className="card"><ShieldCheck className="goldIcon"/><h3>{profile.kyc_status==='verified'?'Marketplace unlocked':'Marketplace locked'}</h3><p className="muted">Posting and accepting tasks requires verified identity.</p></div><div className="card"><BriefcaseBusiness className="goldIcon"/><h3>{profile.role==='runner'||profile.role==='admin'?'Runner profile enabled':'Earn with your time'}</h3>{profile.role==='customer'&&<button disabled={runnerBusy} onClick={enableRunner} className="btn btn-gold btn-wide">Enable runner mode</button>}</div><div className="card"><UserRound className="goldIcon"/><button onClick={signOut} className="btn btn-dark btn-wide"><LogOut size={16}/> Sign out</button></div></aside></div></AppShell>}
