import Link from 'next/link';import {DemoTask} from '@/lib/demo-data';import StatusBadge from './StatusBadge';
export default function TaskCard({task}:{task:DemoTask}){return <Link href={'/tasks/'+task.id} className="taskCard card"><div className="taskCardTop"><div><span className="eyebrow">{task.category}</span><h3>{task.title}</h3></div><StatusBadge status={task.status}/></div><p className="muted clamp2">{task.description}</p><div className="taskMeta"><span>⌖ {task.location}</span><span>◷ {task.deadline}</span></div><div className="taskCardBottom"><b>{task.budget.toLocaleString()} RWF</b><span className="gold">View task →</span></div></Link>}
