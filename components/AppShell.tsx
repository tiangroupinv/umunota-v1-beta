'use client';
import Link from 'next/link';
import {usePathname} from 'next/navigation';
import {useEffect,useState} from 'react';
import {Bell, CircleUserRound, Clock3, CreditCard, Home, MapPin, MessageSquareText, Search, ShieldCheck, UsersRound, ClipboardList, Plus, ChevronDown, MoreHorizontal, X} from 'lucide-react';
import {createClient} from '@/lib/supabase';
import InstallBanner from './InstallBanner';

const nav=[[Home,'Home','/dashboard'],[Plus,'Post task','/tasks/new'],[UsersRound,'Find runner','/runners'],[ClipboardList,'My tasks','/tasks'],[MessageSquareText,'Community','/community'],[CreditCard,'Payments','/payments'],[ShieldCheck,'Trust & KYC','/kyc'],[CircleUserRound,'Profile','/profile']] as const;
const mobilePrimary=[[Home,'Home','/dashboard'],[Plus,'Post','/tasks/new'],[ClipboardList,'Tasks','/tasks'],[MessageSquareText,'Community','/community']] as const;
const mobileMore=[[UsersRound,'Find runner','/runners'],[CreditCard,'Payments','/payments'],[ShieldCheck,'Trust & KYC','/kyc'],[CircleUserRound,'Profile','/profile']] as const;
const areas=['Kigali','Gasabo','Kicukiro','Nyarugenge','Musanze','Huye','Rubavu'];

export default function AppShell({children}:{children:React.ReactNode}){
 const pathname=usePathname();const [search,setSearch]=useState('');const [area,setArea]=useState('Kigali');const [name,setName]=useState('Account');const [moreOpen,setMoreOpen]=useState(false);
 useEffect(()=>{setMoreOpen(false)},[pathname]);
 useEffect(()=>{(async()=>{try{const supabase=createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return;const {data}=await supabase.from('profiles').select('full_name').eq('id',user.id).single();if(data?.full_name)setName(data.full_name)}catch{}})()},[]);
 const initials=name==='Account'?'U':name.split(' ').map(x=>x[0]).slice(0,2).join('').toUpperCase();
 const isActive=(href:string)=>pathname===href||(href!='/tasks'&&pathname.startsWith(href+'/'));
 return <div className="shell"><aside className="sidebar"><Link href="/dashboard" className="logo simpleBrand"><Clock3 size={24}/><span><b>UMUNOTA</b><small>People. Time. Solutions.</small></span></Link><nav className="nav">{nav.map(([Icon,label,href])=><Link className={isActive(href)?'active':''} href={href} key={href}><span className="navIcon"><Icon size={18}/></span><span className="label">{label}</span></Link>)}</nav><div className="sideTrust"><ShieldCheck size={18}/><div><b>Verified marketplace</b><small>Identity · tracked work · protected payments</small></div></div></aside><main className="main"><InstallBanner/><header className="topbar"><form className="searchBox" onSubmit={e=>{e.preventDefault();location.href='/tasks/new?title='+encodeURIComponent(search)}}><Search size={18}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="What do you need done?"/><button>Search</button></form><div className="locationSelect"><MapPin size={17}/><select value={area} onChange={e=>setArea(e.target.value)} aria-label="Location">{areas.map(x=><option key={x}>{x}</option>)}</select><ChevronDown size={14}/></div><button className="iconButton" aria-label="Notifications"><Bell size={18}/></button><Link className="profileChip" href="/profile"><span className="avatar smallAvatar">{initials}</span><span className="profileName">{name}</span></Link></header>{children}</main><nav className="mobileBottomNav">{mobilePrimary.map(([Icon,label,href])=><Link href={href} className={isActive(href)?'active':''} key={href}><Icon size={20}/><span>{label}</span></Link>)}<button className={moreOpen?'active':''} onClick={()=>setMoreOpen(v=>!v)} aria-label="More navigation"><MoreHorizontal size={21}/><span>More</span></button></nav>{moreOpen&&<div className="mobileNavSheetBackdrop" onClick={()=>setMoreOpen(false)}><section className="mobileNavSheet" onClick={e=>e.stopPropagation()}><div className="mobileNavSheetHead"><div><b>More from UMUNOTA</b><small>Account, payments and marketplace tools</small></div><button onClick={()=>setMoreOpen(false)} aria-label="Close"><X size={20}/></button></div><div className="mobileNavSheetGrid">{mobileMore.map(([Icon,label,href])=><Link href={href} className={isActive(href)?'active':''} key={href}><Icon size={21}/><span>{label}</span></Link>)}</div></section></div>}</div>;
}
